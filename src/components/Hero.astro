<section class="hero">
  <div class="hero-bg-gradient"></div>
  <div class="hero-decoration hero-decoration-1"></div>
  <div class="hero-decoration hero-decoration-2"></div>

  <div class="hero-content">
    <div class="hero-text">
      <div class="hero-badge">
        <img src="/favicon.png" alt="" class="hero-badge-logo" width="28" height="28" aria-hidden="true" />
        Piscina temperada y hostal en el sur de Quito
      </div>

      <h1 class="hero-title">
        Tu escape ideal de fin de semana empieza en <span class="title-highlight">Rey de Francia - Piscina y Hostal</span>
      </h1>

      <p class="hero-subtitle">
        Disfruta piscina temperada, sauna, turco, hidromasaje y habitaciones confortables en un ambiente seguro, limpio y con atención cercana para toda la familia.
      </p>

      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">7+</span>
          <span class="stat-label">Servicios de relajación</span>
        </div>
        <div class="stat">
          <span class="stat-number">4.8/5</span>
          <span class="stat-label">Valoración de clientes</span>
        </div>
        <div class="stat">
          <span class="stat-number">365</span>
          <span class="stat-label">Días de asesoría por WhatsApp</span>
        </div>
      </div>

      <div class="hero-ctas">
        <a
          href="https://wa.me/593992577409?text=Hola,%20me%20gustaría%20reservar%20en%20Manantial%20Rey%20de%20Francia"
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-whatsapp btn-lg"
        >
          <i class="bi bi-whatsapp"></i>
          Reserva por WhatsApp
        </a>
        <a href="#reservas" class="btn btn-secondary">
          <i class="bi bi-calendar-check"></i>
          Ver formulario
        </a>
      </div>

      <p class="hero-trust">
        <span><i class="bi bi-shield-check"></i> Higiene garantizada</span>
        <span><i class="bi bi-people"></i> Atención familiar</span>
        <span><i class="bi bi-calendar-week"></i> Jueves a domingo</span>
      </p>
    </div>

    <div class="hero-visual">
      <div class="hero-image-wrapper">
        <div class="image-frame">
          <img
            src="/assets/images/piscina.webp"
            alt="Vista principal de Rey de Francia - Piscina y Hostal"
            loading="eager"
          />
        </div>
        <div class="hero-floating-card">
          <i class="bi bi-whatsapp"></i>
          <div>
            <strong>Respuesta rápida</strong>
            <span>Confirmamos tu reserva en minutos</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <a href="#servicios" class="scroll-indicator">
    <span>Descubre todos nuestros servicios</span>
    <i class="bi bi-chevron-down"></i>
  </a>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding-top: 0;
    overflow: hidden;
    background: var(--color-ivory);
  }

  .hero-bg-gradient {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(900px 600px at 85% 8%, rgba(86, 177, 227, 0.22), transparent),
      radial-gradient(700px 500px at 0% 70%, rgba(26, 62, 122, 0.12), transparent),
      linear-gradient(160deg, #F5F2E8 0%, #EEF4FA 55%, #F5F2E8 100%);
    z-index: 0;
  }

  .hero-decoration {
    position: absolute;
    border-radius: 50%;
    pointer-events: none;
    z-index: 0;
  }

  .hero-decoration-1 {
    width: 320px;
    height: 320px;
    background: radial-gradient(circle, rgba(207, 169, 75, 0.15) 0%, transparent 70%);
    top: -80px;
    right: 8%;
    animation: float-bubble 9s ease-in-out infinite;
  }

  .hero-decoration-2 {
    width: 240px;
    height: 240px;
    background: radial-gradient(circle, rgba(86, 177, 227, 0.2) 0%, transparent 70%);
    bottom: 80px;
    left: 3%;
    animation: float-bubble 11s ease-in-out infinite reverse;
  }

  @keyframes float-bubble {
    0%, 100% { transform: translateY(0) scale(1); opacity: 0.5; }
    50% { transform: translateY(-24px) scale(1.05); opacity: 0.8; }
  }

  .hero-content {
    max-width: 1280px;
    margin: 0 auto;
    padding: var(--spacing-2xl) clamp(1rem, 3vw, 2.5rem);
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: center;
    position: relative;
    z-index: 1;
    width: 100%;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    background: rgba(26, 62, 122, 0.08);
    color: var(--color-royal);
    padding: 0.48rem 1rem 0.48rem 0.65rem;
    border-radius: 999px;
    font-size: 0.86rem;
    font-weight: 600;
    border: 1px solid rgba(207, 169, 75, 0.3);
    width: fit-content;
  }

  .hero-badge-logo {
    object-fit: contain;
    filter:
      drop-shadow(0 2px 6px rgba(207, 169, 75, 0.35))
      drop-shadow(0 4px 12px rgba(207, 169, 75, 0.20));
  }

  .hero-title {
    font-size: clamp(2rem, 4.5vw, 3.2rem);
    color: var(--color-deep);
    line-height: 1.12;
    font-weight: 700;
  }

  .title-highlight {
    background: var(--gradient-gold);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.08rem;
    color: var(--color-stone-500);
    line-height: 1.78;
    max-width: 540px;
  }

  .hero-stats {
    display: flex;
    gap: clamp(1rem, 3vw, 2rem);
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    min-width: 0;
    padding-right: clamp(0.5rem, 2vw, 1.5rem);
    border-right: 1px solid var(--color-stone-200);
  }

  .stat:last-child { border-right: none; }

  .stat-number {
    font-size: 1.75rem;
    color: var(--color-royal);
    font-weight: 700;
    font-family: var(--font-serif);
  }

  .stat-label {
    font-size: 0.82rem;
    color: var(--color-stone-500);
    font-weight: 500;
    line-height: 1.35;
  }

  .hero-ctas {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
  }

  .btn {
    padding: 0.95rem 1.75rem;
    border-radius: var(--radius-lg);
    font-weight: 700;
    font-size: 0.98rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: transform var(--transition-standard), box-shadow var(--transition-standard), background-color var(--transition-standard), color var(--transition-standard), border-color var(--transition-standard);
    text-align: center;
    cursor: pointer;
    text-decoration: none;
  }

  .btn-lg { padding: 1.05rem 2rem; font-size: 1.02rem; }
  .btn i { font-size: 1.15rem; }

  .btn-secondary {
    background: var(--color-white);
    color: var(--color-deep);
    border: 1.5px solid rgba(11, 29, 58, 0.14);
    box-shadow: var(--shadow-soft);
  }

  .btn-secondary:hover {
    background: var(--gradient-blue);
    color: var(--color-white);
    transform: translateY(-3px);
    border-color: transparent;
    box-shadow: 0 14px 32px rgba(26, 62, 122, 0.28);
  }

  .hero-trust {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.25rem;
    font-size: 0.88rem;
    color: var(--color-stone-600);
    line-height: 1.5;
  }

  .hero-trust span {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
  }

  .hero-trust i { color: var(--color-gold); font-size: 0.95rem; }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .hero-image-wrapper {
    position: relative;
    width: 100%;
  }

  .image-frame {
    width: 100%;
    height: 660px;
    aspect-ratio: 5 / 4;
    border-radius: var(--radius-2xl);
    overflow: hidden;
    border: 3px solid rgba(255, 255, 255, 0.85);
    box-shadow:
      0 24px 56px rgba(11, 29, 58, 0.18),
      0 0 0 1px rgba(26, 62, 122, 0.08);
  }

  .image-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 1.2s ease;
  }

  .hero-image-wrapper:hover .image-frame img {
    transform: scale(1.04);
  }

  .hero-floating-card {
    position: absolute;
    bottom: -18px;
    left: -18px;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(37, 211, 102, 0.25);
    border-radius: var(--radius-lg);
    padding: 0.85rem 1.1rem;
    box-shadow: var(--shadow-green);
    max-width: 260px;
  }

  .hero-floating-card i {
    font-size: 1.6rem;
    color: var(--color-whatsapp);
    flex-shrink: 0;
  }

  .hero-floating-card strong {
    display: block;
    font-size: 0.88rem;
    color: var(--color-deep);
    font-family: var(--font-sans);
    font-weight: 700;
  }

  .hero-floating-card span {
    font-size: 0.76rem;
    color: var(--color-stone-500);
    line-height: 1.35;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 28px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
    color: var(--color-stone-500);
    font-size: 0.82rem;
    font-weight: 600;
    z-index: 2;
    animation: bounce 2.5s ease-in-out infinite;
    text-decoration: none;
  }

  .scroll-indicator i { font-size: 1.2rem; color: var(--color-pool); }

  @keyframes bounce {
    0%, 100% { opacity: 0.55; transform: translateX(-50%) translateY(0); }
    50% { opacity: 1; transform: translateX(-50%) translateY(6px); }
  }

  @media (max-width: 900px) {
    .hero { min-height: auto; padding: var(--spacing-xl) 0 var(--spacing-2xl); }
    .hero-content {
      grid-template-columns: 1fr;
      padding: var(--spacing-xl) clamp(1rem, 3vw, 2rem);
      gap: var(--spacing-xl);
    }
    .hero-visual { order: -1; }
    .hero-floating-card {
      bottom: -12px;
      left: 50%;
      transform: translateX(-50%);
      max-width: 90%;
    }
    .hero-ctas { flex-direction: column; }
    .btn { width: 100%; }
    .scroll-indicator { display: none; }
    .hero-decoration { display: none; }
    .stat { border-right: none; padding-right: 0; }
    .hero-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 0.75rem;
    }
  }

  @media (max-width: 520px) {
    .hero-stats { grid-template-columns: 1fr 1fr; }
    .stat:nth-child(3) { grid-column: 1 / -1; }
    .image-frame { aspect-ratio: 4 / 3; border-radius: var(--radius-xl); }
  }
</style>
