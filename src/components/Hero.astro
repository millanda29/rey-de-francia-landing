<section class="hero">
  <div class="hero-decoration hero-decoration-1"></div>
  <div class="hero-decoration hero-decoration-2"></div>
  <div class="hero-decoration hero-decoration-3"></div>

  <div class="hero-content">
    <div class="hero-text">
      <div class="hero-badge">Piscina temperada y hostal en el sur de Quito</div>

      <h1 class="hero-title">
        Tu escape ideal de fin de semana empieza en <span class="title-highlight">Manantial Rey de Francia</span>
      </h1>

      <p class="hero-subtitle">
        Disfruta piscina temperada, sauna, turco, hidromasaje y habitaciones confortables en un ambiente seguro, limpio y con atención cercana para toda la familia.
      </p>

      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">7+</span>
          <span class="stat-label">Servicios de relajación</span>
        </div>
        <div class="stat">
          <span class="stat-number">4.8/5</span>
          <span class="stat-label">Valoración de clientes</span>
        </div>
        <div class="stat">
          <span class="stat-number">365</span>
          <span class="stat-label">Días de asesoría por WhatsApp</span>
        </div>
      </div>

      <div class="hero-ctas">
        <a href="#reservas" class="btn btn-primary btn-lg">
          <span class="btn-icon">→</span>
          Reserva tu plan hoy
        </a>
        <a href="#instalaciones" class="btn btn-secondary">
          Conoce nuestras instalaciones
        </a>
      </div>

      <p class="hero-trust">✓ Protocolos de higiene activos • ✓ Staff capacitado • ✓ Planes para familia, pareja y grupos</p>
    </div>

    <div class="hero-visual">
      <div class="hero-image-wrapper">
        <div class="image-shadow"></div>
        <div class="placeholder-image">
          <img
            src="/assets/images/fachada.webp"
            alt="Vista principal de Manantial Rey de Francia Hostal y Piscinas"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>

  <div class="scroll-indicator">
    <span>Descubre todos nuestros servicios</span>
    <div class="scroll-arrow">↓</div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    background:
      radial-gradient(980px 640px at 88% 12%, rgba(72, 185, 255, 0.16), transparent),
      radial-gradient(860px 620px at 0% 62%, rgba(6, 126, 255, 0.18), transparent),
      linear-gradient(132deg, #f8fbff 0%, #edf3fa 58%, #f4f8fd 100%);
    overflow: hidden;
  }

  .hero-decoration {
    position: absolute;
    border-radius: 50%;
    opacity: 0;
    animation: float-bubble 8s ease-in-out infinite;
  }

  .hero-decoration-1 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(72, 185, 255, 0.3) 0%, transparent 70%);
    top: -50px;
    right: 10%;
    animation-delay: 0s;
  }

  .hero-decoration-2 {
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(6, 126, 255, 0.24) 0%, transparent 70%);
    bottom: 100px;
    left: 5%;
    animation-delay: 2s;
  }

  .hero-decoration-3 {
    width: 250px;
    height: 250px;
    background: radial-gradient(circle, rgba(8, 84, 197, 0.24) 0%, transparent 70%);
    top: 50%;
    right: -50px;
    animation-delay: 4s;
  }

  @keyframes float-bubble {
    0%, 100% { opacity: 0.3; transform: translateY(0px) scale(1); }
    50% { opacity: 0.6; transform: translateY(-30px) scale(1.1); }
  }

  .hero-content {
    max-width: 1520px;
    margin: 0 auto;
    padding: var(--spacing-2xl) clamp(1rem, 2.5vw, 2rem);
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(1.4rem, 2.6vw, 2.8rem);
    align-items: stretch;
    position: relative;
    z-index: 1;
    width: 100%;
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
    justify-content: center;
  }

  .hero-badge {
    display: inline-block;
    background: linear-gradient(135deg, rgba(6, 126, 255, 0.15) 0%, rgba(72, 185, 255, 0.2) 100%);
    color: var(--color-accent-dark);
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-size: 0.9rem;
    font-weight: 600;
    border: 1px solid rgba(6, 126, 255, 0.3);
    width: fit-content;
    animation: slideIn 0.8s ease-out;
  }

  @keyframes slideIn {
    from { opacity: 0; transform: translateX(-20px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .hero-title {
    font-size: 3.35rem;
    color: var(--color-primary);
    line-height: 1.15;
    font-weight: 700;
    animation: slideUp 1s ease-out;
  }

  .title-highlight {
    background: linear-gradient(135deg, var(--color-accent-dark) 0%, var(--color-accent) 48%, var(--color-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes slideUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-subtitle {
    font-size: 1.16rem;
    color: var(--color-text-light);
    line-height: 1.82;
    animation: slideUp 1.2s ease-out;
  }

  .hero-stats { display: flex; gap: var(--spacing-xl); animation: slideUp 1.4s ease-out; }
  .stat { display: flex; flex-direction: column; gap: 0.25rem; }
  .stat-number { font-size: 1.82rem; color: var(--color-accent-dark); font-weight: 700; font-family: var(--font-serif); }
  .stat-label { font-size: 0.9rem; color: var(--color-text-lighter); font-weight: 500; }

  .hero-ctas { display: flex; gap: var(--spacing-md); flex-wrap: wrap; animation: slideUp 1.6s ease-out; }

  .btn {
    padding: 1rem 2rem;
    border-radius: var(--radius-lg);
    font-weight: 600;
    font-size: 1rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: transform var(--transition-standard), box-shadow var(--transition-standard), background-color var(--transition-standard), color var(--transition-standard), border-color var(--transition-standard);
    text-align: center;
    cursor: pointer;
  }

  .btn-lg { padding: 1.1rem 2.5rem; font-size: 1.05rem; }
  .btn-icon { display: inline-block; transition: transform 0.3s ease; }

  .btn-primary {
    background: linear-gradient(135deg, var(--color-accent-dark) 0%, var(--color-accent) 50%, var(--color-secondary) 100%);
    color: var(--color-white);
    box-shadow: 0 14px 32px rgba(8, 84, 197, 0.3);
    border: none;
  }

  .btn-primary:hover { transform: translateY(-4px) scale(1.01); box-shadow: 0 20px 38px rgba(8, 84, 197, 0.36); }
  .btn-primary:hover .btn-icon { transform: translateX(4px); }

  .btn-secondary {
    background: var(--color-white);
    color: var(--color-primary);
    border: 1px solid rgba(11, 31, 54, 0.16);
    box-shadow: 0 10px 26px rgba(11, 31, 54, 0.12);
  }

  .btn-secondary:hover {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    color: var(--color-white);
    transform: translateY(-4px);
    border-color: transparent;
    box-shadow: 0 14px 32px rgba(11, 31, 54, 0.26);
  }

  .hero-trust { font-size: 0.95rem; color: var(--color-text); animation: slideUp 1.8s ease-out; line-height: 1.7; }

  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    animation: slideInRight 1s ease-out;
    height: 100%;
  }

  @keyframes slideInRight {
    from { opacity: 0; transform: translateX(50px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .hero-image-wrapper {
    position: relative;
    width: 100%;
    max-width: none;
    height: 100%;
  }

  .image-shadow {
    position: absolute;
    bottom: -20px;
    left: 50%;
    transform: translateX(-50%);
    width: 90%;
    height: 40px;
    background: radial-gradient(ellipse at center, rgba(15, 23, 42, 0.15) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(10px);
  }

  .placeholder-image {
    width: 100%;
    height: 100%;
    min-height: 520px;
    aspect-ratio: 6 / 5;
    animation: float 6s ease-in-out infinite;
    filter: drop-shadow(0 24px 48px rgba(11, 31, 54, 0.2));
    border-radius: 26px;
    border: 1px solid rgba(255, 255, 255, 0.7);
    overflow: hidden;
  }

  .placeholder-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transform: scale(1.02);
    transition: transform 1200ms ease;
  }

  .hero-image-wrapper:hover .placeholder-image img {
    transform: scale(1.06);
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-25px); }
  }

  .scroll-indicator {
    position: absolute;
    bottom: 30px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
    color: var(--color-text-lighter);
    font-size: 0.9rem;
    animation: bounce 2s ease-in-out infinite;
    z-index: 2;
  }

  .scroll-arrow { font-size: 1.5rem; animation: slideDown 1.5s ease-in-out infinite; }

  @keyframes slideDown {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(10px); }
  }

  @keyframes bounce {
    0%, 100% { opacity: 0.5; }
    50% { opacity: 1; }
  }

  @media (max-width: 768px) {
    .hero { min-height: auto; padding: var(--spacing-xl) 0; }
    .hero-content { grid-template-columns: 1fr; padding: var(--spacing-xl) var(--spacing-md); gap: var(--spacing-xl); }
    .hero-title { font-size: 2.26rem; }
    .hero-subtitle { font-size: 1.06rem; }
    .hero-stats { gap: var(--spacing-lg); flex-wrap: wrap; }
    .hero-visual { order: -1; }
    .placeholder-image { max-width: 100%; min-height: 300px; }
    .hero-ctas { flex-direction: column; }
    .btn { width: 100%; }
    .scroll-indicator { display: none; }
    .hero-decoration { display: none; }
  }

  @media (max-width: 460px) {
    .hero-content {
      padding: 1.1rem 0.8rem 1.4rem;
      gap: 1rem;
    }

    .hero-badge {
      font-size: 0.74rem;
      padding: 0.38rem 0.7rem;
    }

    .hero-title {
      font-size: 1.78rem;
    }

    .hero-subtitle {
      font-size: 0.97rem;
      line-height: 1.7;
    }

    .hero-stats {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.7rem;
    }

    .stat-number {
      font-size: 1.4rem;
    }

    .stat-label {
      font-size: 0.78rem;
      line-height: 1.3;
    }

    .btn,
    .btn-lg {
      font-size: 0.92rem;
      padding: 0.86rem 1rem;
    }

    .hero-trust {
      font-size: 0.84rem;
      line-height: 1.55;
    }

    .placeholder-image {
      min-height: 230px;
    }
  }

  @media (max-width: 260px) {
    .hero-content {
      padding: 0.8rem 0.5rem 1rem;
      gap: 0.7rem;
    }

    .hero-badge {
      font-size: 0.66rem;
      padding: 0.3rem 0.52rem;
    }

    .hero-title {
      font-size: 1.44rem;
      line-height: 1.14;
    }

    .hero-subtitle {
      font-size: 0.86rem;
      line-height: 1.52;
    }

    .hero-stats {
      grid-template-columns: 1fr;
      gap: 0.45rem;
    }

    .stat-number {
      font-size: 1.14rem;
    }

    .stat-label {
      font-size: 0.72rem;
    }

    .hero-ctas {
      gap: 0.5rem;
    }

    .btn,
    .btn-lg {
      font-size: 0.82rem;
      padding: 0.72rem 0.75rem;
      border-radius: 0.7rem;
    }

    .hero-trust {
      font-size: 0.76rem;
      line-height: 1.45;
    }

    .placeholder-image {
      min-height: 180px;
      border-radius: 16px;
    }
  }
</style>
