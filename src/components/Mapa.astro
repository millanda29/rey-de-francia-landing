<section class="ubicacion" id="ubicacion">
  <div class="container">
    <div class="section-header">
      <h2>Estamos ubicados en Chillogallo, Quito</h2>
      <p>Llega fácil y rápido. Encuéntranos en Google Maps y ven a disfrutar tu día de relax.</p>
    </div>

    <div class="map-wrapper">
      <iframe
        title="Ubicación Manantial Rey de Francia en Google Maps"
        src="https://www.google.com/maps/embed?pb=!1m14!1m8!1m3!1d31918.1347647633!2d-78.554826!3d-0.291044!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x91d598b1cc7d0d77%3A0x747671b3475ed43e!2sManantial%20Rey%20de%20Francia!5e0!3m2!1ses-419!2sus!4v1774273098360!5m2!1ses-419!2sus"
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        allowfullscreen
      ></iframe>
    </div>
  </div>
</section>

<style>
  .ubicacion { padding: var(--spacing-2xl) var(--spacing-lg); background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); }
  .container { max-width: 1280px; margin: 0 auto; }
  .section-header { text-align: center; margin-bottom: var(--spacing-xl); }
  .section-header h2 { font-size: 2.5rem; color: var(--color-white); margin-bottom: var(--spacing-sm); }
  .section-header p { color: #cbd5e1; font-size: 1.1rem; }

  .map-wrapper {
    overflow: hidden;
    border-radius: var(--radius-xl);
    border: 3px solid var(--color-accent);
    box-shadow: 0 14px 35px rgba(2, 6, 23, 0.45);
    margin-bottom: var(--spacing-xl);
  }

  .map-wrapper iframe { width: 100%; height: 460px; border: 0; display: block; }

  @media (max-width: 768px) {
    .section-header h2 { font-size: 1.875rem; }
    .map-wrapper iframe { height: 360px; }
  }

  @media (max-width: 460px) {
    .ubicacion {
      padding: var(--spacing-xl) 0.8rem;
    }

    .container {
      padding: 0;
    }

    .section-header h2 {
      font-size: 1.48rem;
      line-height: 1.2;
    }

    .section-header p {
      font-size: 0.95rem;
      line-height: 1.55;
    }

    .map-wrapper {
      border-width: 2px;
      border-radius: 0.8rem;
      margin-bottom: 0.9rem;
    }

    .map-wrapper iframe {
      height: 280px;
    }
  }

  @media (max-width: 260px) {
    .ubicacion {
      padding: var(--spacing-lg) 0.5rem;
    }

    .section-header h2 {
      font-size: 1.2rem;
    }

    .section-header p {
      font-size: 0.78rem;
      line-height: 1.35;
    }

    .map-wrapper iframe {
      height: 220px;
    }
  }
</style>

